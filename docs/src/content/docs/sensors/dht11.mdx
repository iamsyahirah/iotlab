---
title: DHT11 Sensor Setup
description: Discover other Starlight plugins, components and tools developed by HiDeoo.
---

import dhtImage from '../../../assets/sensor-dht11.png';


<img
  src={dhtImage.src}
  alt="DHT11 Temperature and Humidity Sensor"
  width="300"
/>

import { Steps } from '@astrojs/starlight/components'

<Steps>

1. Connect the DHT11 Sensor to the ESP32

    Connect the DHT11 sensor to the ESP32. Make sure the wiring matches the type of DHT11 module you are using.



    | DHT11      | ESP32    | 
    | ---------- | -------- |
    | VIN / VCC  | 3.3V     | 
    | GND        | GND      | 
    | Signal/OUT | GPIO 5   | 

2. Install the DHT library

    In Arduino IDE, go to **Sketch → Include Library → Manage Libraries**

    Search for **DHT sensor library**. Install **DHT sensor library by Adafruit**

    Arduino IDE may also ask you to install **Adafruit Unified Sensor**. Install that too.

3. Open Arduino IDE

    Connect your ESP32 to the computer. Then go to **Tools → Board → ESP32 Arduino → your ESP32 board**
    
    Select **ESP32 Dev Module**

    Then choose the correct USB port **Tools → Port → COM...**

4. Test with simple code

    Create **File -> New Sketch** and paste this code.

    ```c++
    #include <DHT.h>

    #define DHTPIN 4
    #define DHTTYPE DHT11

    DHT dht(DHTPIN, DHTTYPE);

    void setup() {
    Serial.begin(115200);

    dht.begin();

    Serial.println("DHT11 Test");
    }

    void loop() {

    float humidity = dht.readHumidity();
    float temperature = dht.readTemperature();

    if (isnan(humidity) || isnan(temperature)) {
    Serial.println("Failed to read from DHT11!");
    delay(2000);
    return;
    }

    Serial.print("Temperature: ");
    Serial.print(temperature);
    Serial.println(" °C");

    Serial.print("Humidity: ");
    Serial.print(humidity);
    Serial.println(" %");

    Serial.println("--------------------");

    delay(2000);
    }
    ```
    Click the arrow **→** icon to upload the code

4. Open Serial Monitor

    Go to **Tools → Serial Monitor**. Set the baud rate to **115200**

    You should see

    ```Text
    DHT11 Test

    Temperature: 28.00 °C
    Humidity: 72.00 %

    --------------------

    Temperature: 28.00 °C
    Humidity: 73.00 %
    ```
    Observe the temperature and humidity readings from the DHT11 sensor. Blow gently on the sensor or hold it in your hand to observe changes in the readings.

</Steps>


